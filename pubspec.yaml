name: civic_connect
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  supabase_flutter: ^2.10.1   # or let `flutter pub add` pick the best compatible version
  flutter_dotenv: ^6.0.0
  exif: ^3.1.4
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  dotted_border: ^2.1.0
  image_picker: ^1.0.7
  geolocator: ^10.1.0
  geocoding: ^3.0.0
  permission_handler: ^11.1.0
  http: ^1.2.1
  flutter_image_compress: ^2.3.0
  path_provider: ^2.1.3
  share_plus: ^7.2.2
  cached_network_image: ^3.3.1
  timeago: ^3.6.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
